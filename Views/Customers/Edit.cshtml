@model ERP.Models.Customer

@{
    ViewData["Title"] = "تعديل بيانات عميل";
}

@section Styles {
    <style>
        .customer-edit-form .product-form-card > .card-header {
            background: linear-gradient(180deg, #eef8ff 0%, #e6f4ff 100%) !important;
            border-bottom: 1px solid #8fd3ff !important;
            color: #0f3554;
        }
    </style>
}

<div class="container product-form-page customer-edit-form mt-3" dir="rtl">
    <h3 class="mb-3">@ViewData["Title"]</h3>

    <form asp-action="Edit" method="post" class="row g-3">
        @Html.AntiForgeryToken()

        <div class="col-12">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        </div>

        <input type="hidden" asp-for="CustomerId" />

        @* الكارت العلوي (يسار): بيانات أساسية *@
        <div class="col-12 col-xl-6">
            <div class="card product-form-card h-100">
                <div class="card-header py-2"><strong>بيانات أساسية</strong></div>
                <div class="card-body row g-3">
                    <div class="col-12 col-md-4">
                        <label class="form-label">كود العميل</label>
                        <input type="text" class="form-control" value="@Model.CustomerId" disabled readonly />
                    </div>
                    <div class="col-12 col-md-8">
                        <label asp-for="CustomerName" class="form-label"></label>
                        <input asp-for="CustomerName" class="form-control" />
                        <span asp-validation-for="CustomerName" class="text-danger"></span>
                    </div>

                    <div class="col-12 col-md-6">
                        <label asp-for="PolicyId" class="form-label">سياسة العميل</label>
                        <select asp-for="PolicyId" class="form-select" asp-items="ViewBag.PolicyList">
                            <option value="">-- بدون سياسة محددة --</option>
                        </select>
                        <span asp-validation-for="PolicyId" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-6">
                        <label asp-for="CreditLimit" class="form-label"></label>
                        <input type="number" step="0.01" name="CreditLimit" id="CreditLimit" value="@(Model.CreditLimit.ToString("0.00", System.Globalization.CultureInfo.InvariantCulture))" class="form-control" />
                        <span asp-validation-for="CreditLimit" class="text-danger"></span>
                    </div>

                    <div class="col-12 col-md-6">
                        <label class="form-label">نوع الطرف</label>
                        <select asp-for="PartyCategory" class="form-select" id="PartyCategorySelect">
                            <option value=""> اختر</option>
                            <option value="Customer">عميل</option>
                            <option value="Supplier">مورد</option>
                            <option value="Employee">موظف</option>
                            <option value="Investor">مستثمر</option>
                            <option value="Bank">بنك</option>
                            <option value="Expense">مصروف</option>
                            <option value="Owner">مالك / شريك</option>
                        </select>
                        <span asp-validation-for="PartyCategory" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-6">
                        <label asp-for="AccountId" class="form-label"></label>
                        <select asp-for="AccountId" class="form-select" asp-items="ViewBag.AccountId" id="AccountId"></select>
                        <span asp-validation-for="AccountId" class="text-danger"></span>
                    </div>

                    <div class="col-12 col-md-6">
                        <label asp-for="UserId" class="form-label">المستخدم المسئول (المندوب)</label>
                        <select asp-for="UserId" class="form-select" asp-items="ViewBag.UserList">
                            <option value="">-- بدون ربط بمستخدم --</option>
                        </select>
                        <span asp-validation-for="UserId" class="text-danger"></span>
                    </div>

                    <div class="col-12 col-md-6 d-flex align-items-end">
                        <div class="product-form-check-inline">
                            <input asp-for="IsActive" class="form-check-input" id="IsActive" />
                            <label asp-for="IsActive" class="form-check-label" for="IsActive"></label>
                        </div>
                    </div>

                    <div class="col-12">
                        <div class="row g-3 align-items-end">
                            <div class="col-12 col-md-5">
                                <div class="product-form-check-inline">
                                    <input asp-for="IsQuotaMultiplierEnabled" class="form-check-input" id="IsQuotaMultiplierEnabled" />
                                    <label class="form-check-label" for="IsQuotaMultiplierEnabled">مضاعفة الكوتة؟</label>
                                </div>
                            </div>
                            <div class="col-12 col-md-7">
                                <label asp-for="QuotaMultiplier" class="form-label">رقم المضاعفة</label>
                                <select asp-for="QuotaMultiplier" class="form-select">
                                    <option value="1">1 × (بدون مضاعفة)</option>
                                    <option value="2">2 ×</option>
                                    <option value="3">3 ×</option>
                                    <option value="4">4 ×</option>
                                    <option value="5">5 ×</option>
                                </select>
                                <span asp-validation-for="QuotaMultiplier" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        @* الكارت العلوي (يمين): بيانات التواصل *@
        <div class="col-12 col-xl-6">
            <div class="card product-form-card h-100">
                <div class="card-header py-2"><strong>بيانات التواصل</strong></div>
                <div class="card-body row g-3">
                    <div class="col-12 col-md-4">
                        <label asp-for="Phone1" class="form-label"></label>
                        <input asp-for="Phone1" class="form-control" />
                        <span asp-validation-for="Phone1" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-4">
                        <label asp-for="Phone2" class="form-label"></label>
                        <input asp-for="Phone2" class="form-control" />
                        <span asp-validation-for="Phone2" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-4">
                        <label asp-for="Whatsapp" class="form-label"></label>
                        <input asp-for="Whatsapp" class="form-control" />
                        <span asp-validation-for="Whatsapp" class="text-danger"></span>
                    </div>

                    <div class="col-12 col-md-6">
                        <label asp-for="OrderContactName" class="form-label">مسئول الطلب</label>
                        <input asp-for="OrderContactName" class="form-control" />
                        <span asp-validation-for="OrderContactName" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-6">
                        <label asp-for="OrderContactPhone" class="form-label">هاتف مسئول الطلب</label>
                        <input asp-for="OrderContactPhone" class="form-control" />
                        <span asp-validation-for="OrderContactPhone" class="text-danger"></span>
                    </div>

                    <div class="col-12">
                        <label asp-for="Notes" class="form-label"></label>
                        <textarea asp-for="Notes" class="form-control" rows="3"></textarea>
                        <span asp-validation-for="Notes" class="text-danger"></span>
                    </div>
                </div>
            </div>
        </div>

        @* كارت 3: بيانات المستندات/التصنيف *@
        <div class="col-12">
            <div class="card product-form-card">
                <div class="card-header py-2"><strong>بيانات المستندات والتصنيف</strong></div>
                <div class="card-body row g-3">
                    <div class="col-12 col-md-6 col-lg-3">
                        <label asp-for="TaxIdOrNationalId" class="form-label"></label>
                        <input asp-for="TaxIdOrNationalId" class="form-control" />
                        <span asp-validation-for="TaxIdOrNationalId" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-6 col-lg-3">
                        <label asp-for="RecordNumber" class="form-label"></label>
                        <input asp-for="RecordNumber" class="form-control" />
                        <span asp-validation-for="RecordNumber" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-6 col-lg-3">
                        <label asp-for="LicenseNumber" class="form-label"></label>
                        <input asp-for="LicenseNumber" class="form-control" />
                        <span asp-validation-for="LicenseNumber" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-6 col-lg-3">
                        <label asp-for="Segment" class="form-label"></label>
                        <input asp-for="Segment" class="form-control" />
                        <span asp-validation-for="Segment" class="text-danger"></span>
                    </div>
                    <div class="col-12 col-md-6 col-lg-3">
                        <label asp-for="LocationCode" class="form-label"></label>
                        <input asp-for="LocationCode" class="form-control" />
                        <span asp-validation-for="LocationCode" class="text-danger"></span>
                    </div>
                </div>
            </div>
        </div>

        @* كارت 4: العنوان والمنطقة *@
        <div class="col-12">
            <div class="card product-form-card">
                <div class="card-header py-2"><strong>العنوان والمنطقة</strong></div>
                <div class="card-body row g-3">
                    <div class="col-12">
                        <label asp-for="Address" class="form-label"></label>
                        <input asp-for="Address" class="form-control" />
                        <span asp-validation-for="Address" class="text-danger"></span>
                    </div>

                    <div class="col-md-4">
                        <label asp-for="GovernorateId" class="form-label"></label>
                        <select asp-for="GovernorateId" class="form-select" asp-items="ViewBag.GovernorateId" id="GovernorateId">
                            <option value="">-- اختر المحافظة --</option>
                        </select>
                        <span asp-validation-for="GovernorateId" class="text-danger"></span>
                    </div>
                    <div class="col-md-4">
                        <label asp-for="DistrictId" class="form-label"></label>
                        <select asp-for="DistrictId" class="form-select" asp-items="ViewBag.DistrictId" id="DistrictId">
                            <option value="">-- اختر الحي / المركز --</option>
                        </select>
                        <span asp-validation-for="DistrictId" class="text-danger"></span>
                    </div>
                    <div class="col-md-4">
                        <label asp-for="AreaId" class="form-label"></label>
                        <select asp-for="AreaId" class="form-select" asp-items="ViewBag.AreaId" id="AreaId">
                            <option value="">-- اختر المنطقة --</option>
                        </select>
                        <span asp-validation-for="AreaId" class="text-danger"></span>
                    </div>

                    <div class="col-md-6">
                        <label asp-for="RouteId" class="form-label">خط السير</label>
                        <select asp-for="RouteId" asp-items="ViewBag.RouteId" class="form-select">
                            <option value="">-- لا خط سير --</option>
                        </select>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-12">
            <button type="submit" class="btn btn-success">حفظ التعديلات</button>
            <a asp-action="Index"
               class="btn btn-secondary open-tab"
               data-tab-id="customers"
               data-tab-title="قائمة العملاء">رجوع للقائمة</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const partySelect = document.getElementById('PartyCategorySelect');
            const accountSelect = document.getElementById('AccountId');
            const governorateSelect = document.getElementById('GovernorateId');
            const districtSelect = document.getElementById('DistrictId');
            const areaSelect = document.getElementById('AreaId');

            if (partySelect && accountSelect) {
                function setDefaultAccount(forceChange) {
                    const party = partySelect.value;
                    if (!party) return;
                    if (!forceChange && accountSelect.value) return;
                    fetch('@Url.Action("GetDefaultAccountForParty", "Customers")?partyCategory=' + encodeURIComponent(party))
                        .then(r => r.ok ? r.json() : null)
                        .then(data => {
                            if (data && data.success && data.accountId != null)
                                accountSelect.value = data.accountId.toString();
                        })
                        .catch(err => console.error('خطأ في تحميل الحساب الافتراضي:', err));
                }
                partySelect.addEventListener('change', function () { setDefaultAccount(true); });
                setDefaultAccount(false);

                accountSelect.addEventListener('change', function () {
                    const accountId = this.value;
                    if (!accountId) return;
                    fetch('@Url.Action("GetPartyCategoryForAccount", "Customers")?accountId=' + encodeURIComponent(accountId))
                        .then(r => r.ok ? r.json() : null)
                        .then(data => {
                            if (data && data.success && data.partyCategory)
                                partySelect.value = data.partyCategory;
                        })
                        .catch(err => console.error('خطأ في جلب نوع الطرف للحساب:', err));
                });
            }

            if (governorateSelect && districtSelect && areaSelect) {
                const districtPlaceholder = '-- اختر الحي / المركز --';
                const areaPlaceholder = '-- اختر المنطقة --';

                function fillDistrictOptions(govId) {
                    districtSelect.innerHTML = '<option value="">' + districtPlaceholder + '</option>';
                    areaSelect.innerHTML = '<option value="">' + areaPlaceholder + '</option>';
                    if (!govId) return;
                    fetch('@Url.Action("GetDistrictsByGovernorate", "Customers")?governorateId=' + encodeURIComponent(govId))
                        .then(r => r.ok ? r.json() : [])
                        .then(arr => {
                            (arr || []).forEach(function (d) {
                                const opt = document.createElement('option');
                                opt.value = d.id;
                                opt.textContent = d.name;
                                districtSelect.appendChild(opt);
                            });
                        })
                        .catch(err => console.error('Error loading districts:', err));
                }

                function fillAreaOptions(distId) {
                    areaSelect.innerHTML = '<option value="">' + areaPlaceholder + '</option>';
                    if (!distId) return;
                    fetch('@Url.Action("GetAreasByDistrict", "Customers")?districtId=' + encodeURIComponent(distId))
                        .then(r => r.ok ? r.json() : [])
                        .then(arr => {
                            (arr || []).forEach(function (a) {
                                const opt = document.createElement('option');
                                opt.value = a.id;
                                opt.textContent = a.name;
                                areaSelect.appendChild(opt);
                            });
                        })
                        .catch(err => console.error('Error loading areas:', err));
                }

                governorateSelect.addEventListener('change', function () {
                    fillDistrictOptions(this.value || null);
                });
                districtSelect.addEventListener('change', function () {
                    fillAreaOptions(this.value || null);
                });
            }
        });
    </script>
}
