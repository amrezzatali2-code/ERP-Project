@model ERP.Models.Category
@{
    ViewData["Title"] = "تعديل فئة";
}
<div class="container" dir="rtl">
    <h2 class="mb-3">تعديل فئة</h2>

    <form asp-action="Edit" method="post" class="row g-3">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="col-md-4">
            <label class="form-label">معرّف الفئة</label>
            <input asp-for="CategoryId" class="form-control" readonly />
        </div>

        <div class="col-md-8">
            <label asp-for="CategoryName" class="form-label">اسم الفئة</label>
            <input asp-for="CategoryName" class="form-control" />
            <span asp-validation-for="CategoryName" class="text-danger"></span>
        </div>

        <div class="col-12 d-flex gap-2">
            <button class="btn btn-primary">حفظ التعديل</button>
            <a class="btn btn-secondary" asp-action="Index">إلغاء</a>
        </div>
    </form>
</div>
